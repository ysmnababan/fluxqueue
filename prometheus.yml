global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "worker"
    file_sd_configs:
      - files:
          - /etc/prometheus/file_sd/worker_targets.json

  - job_name: "api"
    static_configs:
      - targets: ["api:8080"]
